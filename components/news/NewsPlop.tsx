import { styled } from 'next-yak'
import Link from 'next/link'

export const NewsPlop = styled(Link)`
	background: fuchsia;
	position: absolute;
	top: -0.1rem;
	left: -0.15rem;
	width: 0.6rem;
	height: 0.6rem;
	border-radius: 1rem;
`
