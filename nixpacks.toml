[phases.setup]
nixPkgs = ["bun"]
aptPkgs = []

[phases.detect]
provider = "bun"

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bun run start"

[variables]
NODE_ENV = "production"
NIXPACKS_NODE_VERSION = '20'
